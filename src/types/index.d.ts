import type {
  ComponentClass,
  ComponentType,
  CSSProperties,
  FunctionComponent as FC,
  ReactElement
} from "react";

export { ComponentClass, ComponentType, CSSProperties, FC, ReactElement };
